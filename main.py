import tensorflow as tf

from tensorflow.keras import datasets, layers, models
import matplotlib.pyplot as plt
class_names = ['apple', 'banana', 'plum', 'cherry', 'grape',
               'tomato', 'potato', 'pear', 'peach', 'pepper']
